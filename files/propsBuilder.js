/**
 * propsBuilder.js
 *
 * Responsabilidade única: devolver a lista de análises de prop.
 *
 * Nesta versão (Futebol v1) fica sempre vazio — a extração/normalização
 * de props ainda não foi implementada no Analyser. Quando existir,
 * este é o único arquivo que muda.
 *
 * Não faz: não gera prop artificial, não estima jogador/mercado.
 */

export function buildProps() {
  return [];
}
